# URL of the UniAgent backend API
UNIAGENT_API_URL=http://localhost:3069

# API key (must match API_SECRET in backend/.env) — leave empty if auth is disabled
UNIAGENT_API_KEY=

# Required: Private key of the EOA wallet that owns the Universal Account
# WARNING: Keep this secret! Never commit this file with real values.
UA_PRIVATE_KEY=

# Optional: Enable debug output
# DEBUG=true
